name: Build

on:
  pull_request:
    paths:
      - .github/workflows/build.yml
      - WebSocketClient.xcworkspace/xcshareddata/swiftpm/Package.resolved
      - WebSocketClientPackage/Package.swift
      - WebSocketClientPackage/Sources/DependenciesInterfaces/**
      - WebSocketClientPackage/Sources/DependenciesLive/**

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  build-dependencies-live:
    name: Build DependenciesLive target
    runs-on: xcode-27

    env:
      DEVELOPER_DIR: /Applications/Xcode_27.0.app/Contents/Developer
      DESTINATION: "platform=iOS Simulator,OS=27.0,name=iPhone 18 Pro"

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Get SwiftPM originHash
        id: get-swiftpm-origin-hash
        run: |
          ORIGIN_HASH="$(jq -c -r '.originHash' WebSocketClient.xcworkspace/xcshareddata/swiftpm/Package.resolved)"
          echo "ORIGIN_HASH=${ORIGIN_HASH}" >> "${GITHUB_OUTPUT}"

      - name: Cache Swift Packages
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        id: cache-swiftpm
        timeout-minutes: 20
        with:
          path: .swiftpm
          key: ${{ runner.os }}-swiftpm-${{ steps.get-swiftpm-origin-hash.outputs.ORIGIN_HASH }}
          restore-keys: |
            ${{ runner.os }}-swiftpm-${{ steps.get-swiftpm-origin-hash.outputs.ORIGIN_HASH }}
            ${{ runner.os }}-swiftpm-

      - name: Write IDESkipPackagePluginFingerprintValidatation
        run: defaults write com.apple.dt.Xcode IDESkipPackagePluginFingerprintValidatation -bool YES

      - name: Write IDESkipMacroFingerprintValidation
        run: defaults write com.apple.dt.Xcode IDESkipMacroFingerprintValidation -bool YES

      - name: Xcode build for DependenciesLive
        run: |
          set -o pipefail && \
            xcodebuild \
              -scheme DependenciesLive \
              -workspace WebSocketClient.xcworkspace \
              -destination "${DESTINATION}" \
              -clonedSourcePackagesDirPath .swiftpm \
              -disableAutomaticPackageResolution | xcbeautify --renderer github-actions
